# 阶段 5–9 修复后回归验收

日期：2026-08-14
验收范围：桌面牌桌、手机断点、6-max 核心牌局、ICM 场景、结束复盘、玩家评级、缓存发布和自动化回归。

## 结论

本轮验收通过。上一轮阻塞项已修复：

1. 新牌局会清空旧的即时复盘、结束复盘和当前手得分；历史平均分仍保留。
2. 核心牌局的字母花色统一显示为 ♠、♥、♦、♣。
3. 无有效决策样本时显示“— · 样本期”、可信度 0%，不再给出 E 级等确定性评级。
4. 结束复盘中的“策略频率内”会同时检查得分和明确的过度跟注/弃牌/诈唬提示，不再出现低分却标记为策略频率内的矛盾。
5. 6-max 的 ICM 场景现在实际创建 tournament table，并在即时和结束复盘中显示奖池权益、行动后决策价值、泡沫因子及“ICM 近似、不是完整赛事求解器”的边界说明。
6. Service Worker、入口页和核心模块缓存已升级到 v28，避免继续复用旧版本。
7. 手机端重新整理了顶部工具栏，联网更新按钮不再覆盖行动区。

## 自动化结果

- 12/12 回归套件通过。
- 100 手随机/经典场景试玩：100 手、170 个决策、历史保留 100 手；平均决策分 46；过度跟注 21、过度诈唬 76、过度弃牌 34。
- Web 6-max 集成：60 手完成，随机 30 / 自适应 30，策略包 fixture 验证节点 4 个。
- 策略运行时：已验证机器人行动 120 次，resolver 调用 552 次，未验证节点 3 个。
- ICM：两人精确权益 `[0.525, 0.475]`，泡沫因子 `0.7066`，6-max tournament hero ICM `0.16013302`。

## 当前审计步骤

1. 桌面空状态：6-max 座位、位置标签、花色、行动面板和右侧信息区可见；无样本评级为样本期。见 `fix-14-desktop-final-v28.png`。
2. 结束复盘：对手类型揭示，行动线路、频率、权益和问题标签可见；ICM 场景显示 ICM 近似说明。见 `fix-09-icm-final.png`。
3. 新牌局：旧结束复盘被清空，当前手分数回到“—”，右侧显示“先独立判断”。见 `fix-15-reset-v28.png`。
4. 手机断点：牌桌按真实位置纵向重排，花色正常，顶部工具栏分行，联网更新按钮不覆盖行动区。见 `fix-10-mobile-final.png`。
5. 控制台和布局健康检查：最终浏览器页无 error/warning；桌面 `scrollWidth 1425 <= innerWidth 1440`。

## 证据限制

当前没有接入外部真实 solver 数据，因此系统继续把未覆盖节点标为“近似策略”，不能据此宣称完整 GTO 或职业牌手级强度。截图可以验证可见布局和交互状态；键盘焦点顺序、屏幕阅读器语义和真实移动浏览器性能仍需在目标设备上补充测试。
